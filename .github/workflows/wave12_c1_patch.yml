name: Wave 12 C1 Patch Runner
on:
  push:
    branches:
      - feat/blender-wave12-reference-integrity
jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - run: echo wave12-patch-runner-probe
