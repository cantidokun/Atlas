name: Live Conditional Atlas Test

on:
  workflow_dispatch:
    inputs:
      case:
        description: "Conditional Blender scenario"
        required: true
        type: choice
        options:
          - already-correct
          - incorrect

permissions:
  contents: read

jobs:
  live-conditional:
    runs-on: [self-hosted, windows, local-win]
    environment: local-testing

    steps:
      - name: Check out current Atlas main
        uses: actions/checkout@v4
        with:
          ref: main

      - name: Show runner environment
        shell: cmd
        run: |
          echo Runner name: %RUNNER_NAME%
          echo Workspace: %GITHUB_WORKSPACE%
          cd

      - name: Provision deterministic local Blender fixtures
        shell: cmd
        run: |
          powershell.exe -NoProfile -NonInteractive -ExecutionPolicy Bypass -File ".\scripts\provision_conditional_fixtures.ps1"

      - name: Run live conditional Blender harness
        shell: cmd
        run: |
          powershell.exe -NoProfile -NonInteractive -ExecutionPolicy Bypass -Command "python .\live_qwen_conditional_loop.py --case '${{ inputs.case }}'"
