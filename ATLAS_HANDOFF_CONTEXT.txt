ATLAS DEVELOPMENT HANDOFF CONTEXT — SEPTEMBER 16, 2026 END-OF-NIGHT CHECKPOINT (CURRENT)

Status:
DESIGN CLEAR — IMPLEMENTATION NOT STARTED (Blender Extraction Fidelity v1)

Authoritative repository main:
2ec5a84c0b4d82898a0fb8169844ddd5d93668d2

Cleared Blender design branch:
feat/blender-extraction-fidelity-design

Cleared Blender design commit:
e24753b8d41bf7b7e8ee46eb46795ae6a4c4a485

Cleared design document:
planning/blender/BLENDER_EXTRACTION_FIDELITY_COMPLETION_DESIGN.md (design revision 6)

Documentation freeze:
this commit (tip of the design branch; verify with git log, do not trust an embedded hash)

------------------------------------------------------------
0. CURRENT BLENDER CHECKPOINT
------------------------------------------------------------

Blender correction capabilities through Wave 12 are implemented and merged on main (face removal,
winding repair, merge vertex, parent reference, topology intelligence, isolated vertices, duplicate
vertices, unit metadata, object name normalization, collection normalization, empty-mesh handling,
parent-cycle repair). Wave 12 merged as 2ec5a84c via PR #102. No Wave 13 exists; no new correction
capability is authorized.

The cleared milestone is a READ-ONLY PRODUCER-FIDELITY milestone. It covers:
- Blender read-only producer extraction;
- scene/object membership;
- deterministic representative collection;
- visibility sourced from obj.hide_viewport;
- transform extraction including QUATERNION mode;
- vertex and face extraction;
- preservation of source vertex/face order;
- material-slot-name extraction and ordering;
- explicit omission semantics for normals, UVs and local_frame_id;
- deterministic payload encoding;
- deterministic cross-process evidence;
- preservation of the existing digest boundary.

It does NOT deliver: normals fidelity; UV fidelity; local-frame fidelity; per-face material assignments;
complete multi-collection membership; evaluated/modifier geometry; non-mesh geometry; instance-collection
expansion; cross-language byte-identical serialization; schema v2; correction/write-back; persistence
authority; workflow/recovery changes; Unreal changes; optimization changes.

Implementation-critical contract details:
- Quaternion: three layers — RAW PAYLOAD, RAW ObjectModel.rotation (the digest input), NORMALIZED derived
  TransformModel. Copy source values verbatim as (w,x,y,z); never normalize, rescale, reorder,
  hemisphere-flip or round; reject non-finite and all-zero quaternions; no identity fallback; do not
  delegate rejection to world-pose derivation.
- Materials: any OBJECT-linked or otherwise unrepresentable slot state causes whole-field omission; zero
  data slots produce materials: [] only when no OBJECT-linked slot exists; non-empty lists preserve source
  slot order and are never lexically sorted; no partial list; per-face material_index is out of contract.
- Ordering: canonical object ordering; vertices in source index order; faces in source polygon order;
  materials in source slot order; deterministic tests deliberately use non-sorted source order with
  falsification controls.
- Representation: normals/uvs/local_frame_id keys omitted; no null placeholders for those fields;
  collection:null and parent_object_id:null remain valid payload values.
- Visibility: visible = not obj.hide_viewport; collection/view-layer/render visibility do not participate;
  absent or malformed hide_viewport fails closed.

Mandatory preflight before implementation:
enumerate the frozen Blender asset's rotation_mode values read-only. If any object uses a refused mode
(XZY/YXZ/YZX/ZXY/ZYX, AXIS_ANGLE, unreadable): STOP implementation and return to design review. Do not
reinterpret the asset, do not exempt it, do not weaken the producer rule.

Final design evidence:
- final design commit e24753b8d41bf7b7e8ee46eb46795ae6a4c4a485;
- Round-5 documentary audit: 57 checks, 0 failures;
- branch-vs-baseline diff at that commit contains only the design document;
- production implementation files changed by the design revision: 0;
- implementation has not started; no implementation branch exists; no milestone test run yet.

Review provenance:
the last in-repository review verdict was BLOCKED at 2873ac6 (contradictory section 15 null/normalization
requirement; non-falsifiable order-preservation tests). Design revision 6 (e24753b) closes both and passed
the 57-check documentary audit, but no author-independent review of e24753b is recorded in the
repository. Clearance to implement is the human decision-maker's authorization.

D4 working-tree drift (primary tree only, Desktop/Atlas, HEAD 2ec5a84):
8 slots=True removals plus one docstring line across
planning/blender/correction_authorization.py, planning/blender/correction_contract.py and
planning/blender/correction_planner.py. NOT repaired, NOT reverted, NOT staged, NOT committed,
NOT re-pinned; do not touch it incidentally.

Unreal separation:
the next Blender session must not drift into PR #103, Unreal autonomy, Unreal optimization, render-job
recovery, or Unreal branch reconciliation.

Next-session resume procedure (exact order):
a. verify repository/branch state;
b. verify authoritative main = 2ec5a84c0b4d82898a0fb8169844ddd5d93668d2;
c. verify cleared design = e24753b8d41bf7b7e8ee46eb46795ae6a4c4a485;
d. verify the frozen asset's rotation modes read-only;
e. if the preflight passes, implement only the cleared Extraction Fidelity v1 contract;
f. add/run the deterministic tests the design requires;
g. run the required live Blender gate;
h. capture evidence;
i. independently review the implementation;
j. only then consider merge/mainline integration.

Documentation discipline:
use the explicit terminology DESIGN CLEAR / IMPLEMENTATION NOT STARTED. Do not claim any implementation
feature as complete because the design is cleared.

Full record: ATLAS_HANDOFF_2026-09-16_END_OF_NIGHT.md (FINAL CHECKPOINT section).

============================================================
PRESERVED HISTORICAL CHECKPOINT — SEPTEMBER 6, 2026 (VERBATIM, SUPERSEDED)
============================================================

ATLAS DEVELOPMENT HANDOFF CONTEXT — SEPTEMBER 6, 2026 CHECKPOINT

Active repository branch:
main

Current milestone:
M5 — independent Unreal render-evidence verification — COMPLETE
Latest M5 merge commit:
a9b6eb00e62f252cc3aa5b7ef81998797cb12f83

============================================================
1. PROJECT DIRECTION
============================================================

Atlas is an AI-assisted virtual-production and digital-twin platform focused exclusively on soccer-field-related digital twins and their production pipeline.

Dedicated photogrammetry software creates the initial reconstruction. Blender analyzes, cleans, corrects, optimizes, and prepares that reconstruction. Unreal is a downstream controlled production environment around the canonical Atlas Digital Twin.

Atlas owns the canonical Digital Twin. Blender files, Unreal projects/levels, render jobs, receipts, and output artifacts are production representations/state, not canonical identity.

============================================================
2. AUTHORITY MODEL
============================================================

Qwen / AI / development models
    -> reason and propose structured production intent

Python / Atlas
    -> validate, resolve, authorize, execute, track state, verify, recover

Blender / Unreal
    -> controlled production execution

Independent verification
    -> establish what actually happened

Models and agent wrappers never receive direct Atlas production execution or authorization authority.

============================================================
3. BLENDER POSITION
============================================================

Stage 13:
- multi-step partial-progress recovery — COMPLETE FOR CURRENT CONTRACT; live verified.

Stage 14:
- dependency-aware task composition — COMPLETE FOR CURRENT CONTRACT; serial execution and cross-process recovery live verified.

Stage 15:
- semantic soccer-production tasks, reusable fragments/templates, target-state evaluation, versioned catalog, semantic provenance persistence — COMPLETE FOR CURRENT CONTRACT; live verified.

Stage 16:
- Qwen proposal/provider integration — LIVE VERIFIED through proposal, Atlas authorization, real Blender mutation, cross-process recovery, and advisory-only Qwen recovery reasoning.

Stage 17:
- Blender production-artifact lineage closed loop — LIVE VERIFIED against real Blender 4.4.

============================================================
4. UNREAL AUTONOMY EXECUTION BRIDGE
============================================================

PR #59 merged the clean Unreal autonomy execution bridge into main:
- planning/unreal_execution_boundary.py: narrow tool-call to UnrealOperation validation boundary.
- planning/unreal_autonomous_executor.py: ToolExecutor adapter consumed by AutonomousTaskRuntime.
- controller/agent_controller_host.py: host-owned trusted-context wiring to the adapter.
- planning/unreal_transport_contract.py, unreal_transport_serialization.py, unreal_transport_named_pipe.py, unreal_adapter_production.py: controlled production transport layer.

The generic Atlas autonomous runtime remains the single progression authority.

============================================================
5. STAGE 17 — PRODUCTION ARTIFACT LINEAGE
============================================================

Implemented:
- immutable provenance-only ProductionArtifactManifest;
- canonical Digital Twin identity binding;
- engine-specific Blender and Unreal construction bridges;
- symmetric engine identity verification;
- durable ProductionArtifactStore with fail-closed reload validation;
- Unreal lineage requirement for verified inspect_render_job evidence;
- independently observed Unreal artifact-path requirement;
- canonical detached UnrealEvidence snapshot/from_snapshot boundaries;
- canonical detached UnrealRenderReceipt snapshot/from_snapshot boundaries;
- disposable live_unreal_production_artifact_proof.py harness.

Blender Stage 17 is live verified against real Blender 4.4.

Unreal Stage 17 is also LIVE VERIFIED. The real UE 5.6 proof completed successfully after the C++ render-state consistency defect discovered during the first proof attempt was fixed.

The successful Unreal chain is:

UE 5.6 render
    -> inspect_render_job
    -> authoritative evidence verification
    -> verified UnrealEvidence
    -> UnrealRenderReceipt
    -> ProductionArtifactManifest
    -> durable persistence
    -> reload
    -> exact lineage verification

============================================================
6. UNREAL POSITION & RENDER/RECOVERY BOUNDARY
============================================================

PR #57 restored the working Unreal Engine 5.6 execution boundary to main (19/19 build actions succeeded on the UE 5.6 editor build).
PR #59 connected the clean execution bridge to AutonomousTaskRuntime.

The M4 cross-process recovery implementation is now merged to main. The recovery path is Atlas-owned and durable; Unreal remains the controlled worker/witness boundary.

M4 includes:
- durable AtlasRenderJobRecord state;
- durable intent before submission;
- Atlas-generated immutable atlas_job_id and attempt identity;
- coordinator lease/fencing and per-job ownership;
- process/session identity including process creation identity;
- contained Job Object supervision and fail-closed handling of unsupported uncontained attached recovery;
- witness journaling outside Saved;
- attempt nonce/HMAC protections;
- per-attempt output isolation;
- engine-attested output manifests and independent artifact hashing;
- full-catalog reconciliation with stability checks;
- rogue/unmanaged job detection without silent adoption;
- explicit orphan/ambiguity states;
- receipt identity binding and create-if-absent semantics;
- no automatic resubmission after uncertain acceptance.

============================================================
7. M5 — INDEPENDENT EVIDENCE VERIFICATION
============================================================

PR #68 is merged. `verify_render_job_evidence(...)` is the authoritative boundary for converting raw observed Unreal render-job state into verified UnrealEvidence.

M5 requires:
- supported evidence source class;
- a concrete AtlasRenderJobRecord with intact construction/deserialization invariants;
- full identity binding to the durable record;
- exact five-key expected output topology;
- unconditional frame-count enforcement;
- output path isolation and traversal/ADS/device-namespace defenses;
- mandatory engine-attested output manifest;
- exact path-set matching;
- positive sizes and canonical lowercase SHA-256 digests;
- independent disk size/hash verification;
- PNG structure/CRC/IHDR/IEND checks;
- IDAT zlib stream integrity/completeness;
- rejection of unexpected extra disk artifacts.

`verified=True` is never supplied by the transport. It is produced only after Atlas independently verifies the complete evidence/artifact set.

M5 head:
d7ce33194946fde5076871f8f13f2c5f6776d655

Merged:
a9b6eb00e62f252cc3aa5b7ef81998797cb12f83

Validation:
- evidence verification tests: 39 passed;
- recovery coordinator tests: 15 passed;
- pytest -k unreal: 302 passed, 648 deselected;
- full pytest: 950 passed;
- GitHub Actions Atlas Tests: completed successfully for the M5 head.

No action-runner/workflow tests were run.

============================================================
8. CONTROLLER-TO-UNREAL TRUST BOUNDARY
============================================================

For protected Unreal production requests:
- TrustedUnrealContext is the authority source for protected intent;
- host-owned authorization context and sequence path are preserved;
- model-supplied intent cannot replace trusted intent;
- model-supplied production flags cannot disable the host-owned production marker;
- contradictory model values remain diagnostic mismatch state only;
- the Unreal production integration seam rejects missing required trusted context before execution.

The C++ Unreal transport is an execution/transport boundary only. Authority remains owned by the trusted Atlas host boundary.

============================================================
9. MODEL / HERMES DEVELOPMENT STRATEGY
============================================================

Hermes may use a different reasoning provider without changing Atlas authority boundaries.

The next-session experiment under consideration is a controlled comparison of Gemini vs DeepSeek V4 Flash for Hermes-led development, specifically token efficiency versus reasoning quality.

Astra and Claude 5 remain reserved for red-team evaluation so the development-model experiment does not remove an independent adversarial review layer.

ChatGPT may also independently inspect/evaluate Hermes-produced work as an additional review layer. This does not make ChatGPT an Atlas execution authority and does not replace deterministic tests, independent evidence verification, CI, or the human merge decision.

Evaluate the model switch using practical engineering outcomes:
- reasoning quality per token;
- architectural fidelity;
- regression/defect rate;
- test-fix efficiency;
- red-team findings;
- rework and time-to-merge.

Do not optimize for token count alone.

============================================================
10. RESUME POINT
============================================================

Read:
- ATLAS_HANDOFF_CURRENT.md
- README.md
- UNREAL_AGENT_HANDOFF_CURRENT.md
- docs/ATLAS_ARCHITECTURE_CONTRACT.md

Then:
1. Pull latest main.
2. Treat M4 and M5 as completed baseline work; reopen only if a concrete regression is demonstrated.
3. Continue from the next unresolved milestone in the architecture contract.
4. For Hermes, compare Gemini vs DeepSeek V4 Flash while retaining Astra/Claude 5 as red-team evaluators.
5. Keep implementation, deterministic validation, independent verification, red-team review, and human merge authority distinct.

Do not run workflow/action-runner tests unless explicitly authorized.

Historical dated handoff snapshots are archival records and should not be rewritten.