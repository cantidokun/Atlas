ATLAS DEVELOPMENT HANDOFF CONTEXT — SEPTEMBER 4, 2026 END-OF-NIGHT CHECKPOINT

Active repository branch:
main

Current development stage:
Stage 17 — Production artifact lineage — IN PROGRESS

============================================================
1. PROJECT DIRECTION
============================================================

Atlas is an AI-assisted virtual-production and digital-twin platform focused exclusively on soccer-field-related digital twins and their production pipeline.

Dedicated photogrammetry software creates the initial reconstruction. Blender analyzes, cleans, corrects, optimizes, and prepares that reconstruction. Unreal is a downstream controlled production environment around the canonical Atlas Digital Twin.

Atlas owns the canonical Digital Twin. Blender files, Unreal projects/levels, render jobs, receipts, and output artifacts are production representations/state, not canonical identity.

============================================================
2. AUTHORITY MODEL
============================================================

Qwen / AI
    -> reason and propose structured production intent

Python / Atlas
    -> validate, resolve, authorize, execute, track state, verify, recover

Blender / Unreal
    -> controlled production execution

Independent verification
    -> establish what actually happened

Qwen never receives direct production execution or authorization authority.

============================================================
3. BLENDER POSITION
============================================================

Stage 13:
- multi-step partial-progress recovery — COMPLETE FOR CURRENT CONTRACT; live verified.

Stage 14:
- dependency-aware task composition — COMPLETE FOR CURRENT CONTRACT; serial execution and cross-process recovery live verified.

Stage 15:
- semantic soccer-production tasks, reusable fragments/templates, target-state evaluation, versioned catalog, semantic provenance persistence — COMPLETE FOR CURRENT CONTRACT; live verified.

Stage 16:
- Qwen proposal/provider integration — LIVE VERIFIED through proposal, Atlas authorization, real Blender mutation, cross-process recovery, and advisory-only Qwen recovery reasoning.

============================================================
4. STAGE 17 — PRODUCTION ARTIFACT LINEAGE
============================================================

Implemented:
- immutable provenance-only ProductionArtifactManifest;
- canonical Digital Twin identity binding;
- engine-specific Blender and Unreal construction bridges;
- symmetric engine identity verification;
- durable ProductionArtifactStore with fail-closed reload validation;
- Unreal lineage requirement for verified inspect_render_job evidence;
- independently observed Unreal artifact-path requirement;
- canonical detached UnrealEvidence snapshot/from_snapshot boundaries;
- canonical detached UnrealRenderReceipt snapshot/from_snapshot boundaries;
- disposable live_unreal_production_artifact_proof.py harness.

Blender Stage 17 is user-verified against real Blender 4.4.

Unreal Stage 17 provenance is implemented and regression-verified, but the real UE 5.6 provenance proof remains pending human validation.

============================================================
5. UNREAL POSITION
============================================================

PR #57 restored the previously working Unreal Engine 5.6 execution boundary to main and was merged as:
3e2e78e654cab3db5f17ba9739ae5c609d82f386

The restored boundary includes:
- UE 5.6 project and module definitions;
- controlled render fixture assets;
- render and world-save boundary sources;
- commandlets used by the fixture workflow;
- the Unreal transport module and headers;
- the named-pipe Atlas transport server.

Local UE 5.6 compilation of the restored tree succeeded with 19/19 actions, including AtlasTransportServer.cpp, AtlasUnrealTransport.cpp, and the render/world-save boundary sources.

The real Unreal render workflow is:
- deterministic render configuration;
- render-state verification;
- Movie Render Queue submission;
- dynamic job-ID binding;
- asynchronous job inspection;
- semantic completion verification;
- actual output-artifact discovery;
- filesystem existence/non-zero-size validation;
- verified inspect_render_job evidence;
- evidence-bound UnrealRenderReceipt;
- durable receipt persistence.

Stage 17 continues from the already verified pair:

verified Unreal evidence/receipt
    -> manifest construction
    -> durable manifest persistence
    -> reload
    -> exact lineage verification

Cross-process Unreal render-job recovery is NOT implemented. Receipt persistence must not be described as job persistence.

============================================================
6. CONTROLLER-TO-UNREAL TRUST BOUNDARY
============================================================

Current mainline AgentControllerHost remains deliberately narrower than the historical controller-host branch retained in PR #50.

For protected Unreal production requests:
- TrustedUnrealContext is the authority source for protected intent;
- host-owned authorization context and sequence path are preserved;
- model-supplied intent cannot replace trusted intent;
- model-supplied production flags cannot disable the host-owned production marker;
- contradictory model values are retained only as diagnostic mismatch state;
- the Unreal production integration seam rejects missing required trusted context before execution.

The restored C++ Unreal transport is an execution/transport boundary only. Its request-level authorization field is part of transport validation; it is not a replacement for Atlas host authorization.

============================================================
7. TESTING / VALIDATION
============================================================

Verified in this checkpoint:
- UE 5.6 local editor build of restored Unreal tree — 19/19 actions succeeded;
- AtlasTransportServer.cpp compiled and linked;
- AtlasUnrealTransport.cpp compiled and linked;
- render/world-save boundary sources compiled successfully.

Previously verified checkpoints include the deterministic Stage 17 provenance/snapshot suites and the earlier full deterministic regression checkpoint.

The newest September 4 controller trust-boundary changes have NOT received a new local Windows deterministic test result in this checkpoint.

Do not represent older test results as validation of the newest controller changes.

Do not run workflow/action-runner tests unless explicitly authorized.

============================================================
8. NON-REGRESSION RULES
============================================================

- Never give Qwen direct execution or authorization authority.
- Never accept model-supplied authorization IDs, receipts, protected Unreal intent, or protected production flags as Atlas authority.
- Never automatically retry failed writes.
- Never silently mutate an authorized plan.
- Never declare completion from a transport/write response alone.
- Preserve independent verification and the evidence ledger.
- Keep engine-specific behavior behind adapter/tool boundaries.
- Keep dependency-aware execution serial until concurrency is independently justified.
- Preserve canonical Digital Twin identity separately from engine/DCC artifacts.
- Do not claim cross-process Unreal render-job recovery until separately implemented and verified.

============================================================
9. END-OF-NIGHT RESUME POINT
============================================================

Read:
- ATLAS_HANDOFF_CURRENT.md
- README.md
- UNREAL_AGENT_HANDOFF_CURRENT.md
- docs/ATLAS_ARCHITECTURE_CONTRACT.md
- docs/OPENHANDS_TRANSITION_GUIDE.md

Then:
1. Pull latest main.
2. Run focused deterministic tests covering the newest host trust-boundary changes.
3. Exercise the restored UE 5.6 render boundary and capture fresh verified evidence/receipt data.
4. Run live_unreal_production_artifact_proof.py against the verified evidence/receipt pair.
5. Confirm manifest persistence, reload, exact lineage, and digest identities.
6. Resume selective integration of the stronger historical controller-host architecture from PR #50 only after the Stage 17 proof checkpoint.

Historical dated handoff snapshots are archival records and should not be rewritten.